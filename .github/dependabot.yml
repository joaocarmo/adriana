version: 2

updates:
  # Actions are SHA-pinned; Dependabot bumps the SHA and its version comment.
  # The only ecosystem here: the site itself has no dependencies.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
